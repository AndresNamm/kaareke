{
  "name": "kaareke",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "mkdir -p public/content && cp -R content/* public/content/ && vite",
    "build": "mkdir -p public/content && cp -R content/* public/content/ && tsc && vite build && rm -rf build && mv dist build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/marked": "^5.0.2",
    "typescript": "~6.0.2",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "marked": "^18.0.4"
  }
}
